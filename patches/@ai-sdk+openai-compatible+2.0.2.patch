diff --git a/node_modules/@ai-sdk/openai-compatible/dist/index.js b/node_modules/@ai-sdk/openai-compatible/dist/index.js
index 92121b5..eb28eaf 100644
--- a/node_modules/@ai-sdk/openai-compatible/dist/index.js
+++ b/node_modules/@ai-sdk/openai-compatible/dist/index.js
@@ -73,13 +73,16 @@ function convertOpenAICompatibleChatUsage(usage) {
   }
   const promptTokens = (_a = usage.prompt_tokens) != null ? _a : 0;
   const completionTokens = (_b = usage.completion_tokens) != null ? _b : 0;
-  const cacheReadTokens = (_d = (_c = usage.prompt_tokens_details) == null ? void 0 : _c.cached_tokens) != null ? _d : 0;
+  // Only treat cache as known when the provider reports cached_tokens.
+  // Missing details must stay undefined (not coerced to 0 / all-no-cache).
+  const cacheReadTokens = (_c = usage.prompt_tokens_details) == null ? void 0 : _c.cached_tokens;
+  const hasCacheDetails = cacheReadTokens != null;
   const reasoningTokens = (_f = (_e = usage.completion_tokens_details) == null ? void 0 : _e.reasoning_tokens) != null ? _f : 0;
   return {
     inputTokens: {
       total: promptTokens,
-      noCache: promptTokens - cacheReadTokens,
-      cacheRead: cacheReadTokens,
+      noCache: hasCacheDetails ? promptTokens - cacheReadTokens : void 0,
+      cacheRead: hasCacheDetails ? cacheReadTokens : void 0,
       cacheWrite: void 0
     },
     outputTokens: {
diff --git a/node_modules/@ai-sdk/openai-compatible/dist/index.mjs b/node_modules/@ai-sdk/openai-compatible/dist/index.mjs
index 15c2be8..afaa04d 100644
--- a/node_modules/@ai-sdk/openai-compatible/dist/index.mjs
+++ b/node_modules/@ai-sdk/openai-compatible/dist/index.mjs
@@ -53,13 +53,16 @@ function convertOpenAICompatibleChatUsage(usage) {
   }
   const promptTokens = (_a = usage.prompt_tokens) != null ? _a : 0;
   const completionTokens = (_b = usage.completion_tokens) != null ? _b : 0;
-  const cacheReadTokens = (_d = (_c = usage.prompt_tokens_details) == null ? void 0 : _c.cached_tokens) != null ? _d : 0;
+  // Only treat cache as known when the provider reports cached_tokens.
+  // Missing details must stay undefined (not coerced to 0 / all-no-cache).
+  const cacheReadTokens = (_c = usage.prompt_tokens_details) == null ? void 0 : _c.cached_tokens;
+  const hasCacheDetails = cacheReadTokens != null;
   const reasoningTokens = (_f = (_e = usage.completion_tokens_details) == null ? void 0 : _e.reasoning_tokens) != null ? _f : 0;
   return {
     inputTokens: {
       total: promptTokens,
-      noCache: promptTokens - cacheReadTokens,
-      cacheRead: cacheReadTokens,
+      noCache: hasCacheDetails ? promptTokens - cacheReadTokens : void 0,
+      cacheRead: hasCacheDetails ? cacheReadTokens : void 0,
       cacheWrite: void 0
     },
     outputTokens: {
